let month = prompt ("Write desired month for you vacation");
let myMonth = month.toLowerCase();
if (month == 'July' || month == 'August') {
    document.write ('Approved')
}
else {
    document.write ('Deny')
}