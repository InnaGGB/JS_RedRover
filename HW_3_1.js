let numOne = +prompt ('Enter the 1st value');
let numTwo = +prompt ('Enter the 2nd value');
let operator = prompt ('type the operator');
switch (operator) {
    case ('/'):
    numOne !=0;
    numTwo !=0;
    console.log (numOne/numTwo);
    break;
    case ('/'):
    numOne = 0;
    numTwo = 0;
    console.log ("You can't divide by 0");
    break;
    case ('+'):
    console.log ((numOne) + (numTwo));
    break;
    case ('-'):
    console.log (numOne - numTwo);
    break;
    case ('*'):
    console.log (numOne * numTwo);
    break;
}